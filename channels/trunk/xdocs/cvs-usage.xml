<?xml version="1.0" encoding="iso-8859-1" ?>

<document>  
  <properties>
    <title>CVS</title>
  </properties>

  <body>
      <section name="Web Access">
        <p>
          You can view the latest source code through the 
	  <a href="/cvs/cap">CVS web interface</a>.
        </p>
      </section>    
  
      <section name="Developer CVS Access via SSH">

        <p>
          Only project developpers can access the CVS tree via this method.
          SSH2 must be installed on your client machine. 
	  On Linux, install the OpenSSH package and run:</p>
	  
	  <source>
	   ssh-keygen -t dsa -b 2048
</source>

	<p>
	  You may enter a passphrase there if you want to, but remember that
	  if you do, you will be prompted for it everytime you access CVS. Send
	  <a href="mailto:denis@mind-alliance.com">me</a> the file
	  <code>~/.ssh/id_dsa.pub</code> so that I can update your account.
	</p>
	
	<p>
	Once your account is enabled, you should be able to do the following to 
	download a copy of the latest code:
	</p>
	  
        <source>
          export CVS_RSH=ssh
          cvs -z3 -d :ext:user@ranger.homelinux.net:/var/cvs co cap
</source>
	
	<p>
	  Remember to replace <i>user</i> with your actual user name... You
	  may omit the <code>user@</code> if your username on your workstation
	  is the same as your server account name. For your own local builds, you
	  should add the following line to <code>~/build.properties</code>:
        </p>
	<source>
	 maven.username=<i>user</i>
</source>
      </section>
  </body>
</document>

