<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." default="all" name="Channels Model Module">
    
    <target name="init">
        <property name="deploy.dir" value="deploy"/>
        <mkdir dir="${deploy.dir}"/>
    </target>
    
	<target description="Build everything." name="all" depends="init">
		<ant antfile="build-mod-model.xml"/>
		<ant antfile="build-test-model.xml"/>
        <echo message="Channels Model Module build completed."/>
    </target>
    
    <target description="Clean up the derived objects" name="clean">
        <delete>
            <fileset dir="." includes="**/*.jar"/>
        </delete>
    	<delete dir="${deploy-dir}"/>
		<ant antfile="build-mod-model.xml" target="clean"/>
		<ant antfile="build-test-model.xml" target="clean"/>
    </target>
	    
</project>
