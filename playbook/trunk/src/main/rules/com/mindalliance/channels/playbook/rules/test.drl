package com.mindalliance.channels.playbook.rules

import com.mindalliance.channels.playbook.ifm.*

rule "test"
	when
		m : IfmElement( ref : reference, date : createdOn )
	then
	    System.out.println( ref + " was created on " + date );
end
